# Changelog

## 2026.7.20

- Initial release: OODA decision/action-routing plugin. SessionStart primer + a
  conditional UserPromptSubmit nudge (with per-session anti-nag cooldown and a
  thin-PATH `run-python.sh` resolver) that fires on decide/act/iterate/plan/
  compete language and runs Boyd's real loop before the move. `/ooda:loop`
  command for manual invocation. Bundled `ooda` skill carries the real loop
  structure (implicit-guidance shortcuts), Orient as the schwerpunkt (five inputs
  + destruction/creation), tempo-not-speed, and the anti-patterns — Boyd-faithful
  and cited (Boyd's briefings, Osinga, Richards, Coram).
