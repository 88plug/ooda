#!/usr/bin/env bash
# install.sh — ooda has no statusLine and needs no settings wiring; the hooks
# and skill load automatically from .claude-plugin/plugin.json once enabled.
echo "ooda installed — hooks + skill load automatically from the manifest. No extra setup."
